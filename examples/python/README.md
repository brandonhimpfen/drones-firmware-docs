# Python Examples

Small documentation-support tools for firmware teams.

## Commands

```bash
python scripts/validate_manifest.py ../../sample-firmware-manifest.json
python scripts/parameter_summary.py ../../sample-parameter-catalog.json
python scripts/release_check.py ../../sample-firmware-manifest.json
```

The examples use only the Python standard library so they can run in constrained CI environments.
